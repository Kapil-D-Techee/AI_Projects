# Used for local `pip install -r requirements.txt`. Vercel deploys read
# dependencies from pyproject.toml's [project.dependencies] instead — keep
# both lists in sync when adding/removing a package.
fastapi>=0.115
uvicorn[standard]>=0.32
python-dotenv>=1.0
pydantic>=2.9
python-multipart>=0.0.12
httpx>=0.27
groq>=0.13
openai>=1.54
elevenlabs>=1.10
upstash-redis>=1.2
